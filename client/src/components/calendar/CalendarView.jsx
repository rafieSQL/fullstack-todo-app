import ChronosCalendar from '../ChronosCalendar.jsx'

/**
 * CalendarView modular container for Adaptive Timebox Calendar View (#calendar)
 */
export default function CalendarView(props) {
  return <ChronosCalendar {...props} />
}
