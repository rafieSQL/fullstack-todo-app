import FocusSessionComponent from '../FocusSession.jsx'

/**
 * FocusSession modular container for Fullscreen Deep Work Focus Mode (#focus)
 */
export default function FocusSession(props) {
  return <FocusSessionComponent {...props} />
}
